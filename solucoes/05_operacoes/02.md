## Problema

2 - Qual o valor de `x` após as seguintes instruções? Justifica a tua
resposta.

```c
int x;
x = 3 + 4.9;
```

## Soluções

### Solução 1

Após as instruções o valor de `x` é 7 pois `x` é do tipo inteiro, logo a parte decimal é cortada após o cálculo. 